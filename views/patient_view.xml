<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="hospital_patient_list_view" model="ir.ui.view">
            <field name="name">hospital.patient.tree</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <tree string="All Patients">
                    <field name="name" />
                    <field name="age" />
                    <field name="gender" />
                </tree>
            </field>
        </record>

        <record id="hospital_patient_form_view" model="ir.ui.view">
            <field name="name">hospital.patient.form</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <form string="Patient Details">
                    <sheet>
                        <widget name="web_ribbon" title="Archived" bg_color="bg-danger"
                            invisible="active" />
                        <group string="Patient Info">
                            <field name="name" />
                            <field name="age" />
                            <field name="date_of_birth" />
                        </group>
                        <group>
                            <field name="gender" />
                            <field name="active" invisible="1" />
                        </group>

                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" />
                        <field name="activity_ids" />
                        <field name="message_ids" />
                    </div>
                </form>
            </field>
        </record>

        <record id="hospital_patient_search_view" model="ir.ui.view">
            <field name="name">hospital.patient.search</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name" filter_domain="[('name','ilike', self)]" />
                    <field name="age" />
                    <field name="gender" />

                    <filter name="filter_male" string="Male" domain="[('gender', '=', 'male')]" />
                    <filter name="filter_female" string="Female"
                        domain="[('gender', '=', 'female')]" />
                    <separator />
                    <filter name="filter_kids" string="Kids" domain="[('age','&lt;', 18)]" />
                    <separator />
                    <filter name="filter_archive" string="Archived" domain="[('active','=',False)]" />

                    <group expand="0" string="Group By">
                        <filter name="group_by_gender" string="Gender" domain="[]"
                            context="{'group_by': 'gender'}" />
                    </group>
                    <searchpanel>
                        <field name="gender" icon="fa-users" enable_counters="1" />
                    </searchpanel>
                </search>
            </field>
        </record>

        <record id="hospital_patient_action" model="ir.actions.act_window">
            <field name="name">Patients</field>
            <field name="res_model">hospital.patient</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_group_by_gender': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">Hi, Create New Patient!</p>
            </field>
        </record>

        <record id="hospital_kids_patient_action" model="ir.actions.act_window">
            <field name="name">Kids Patients</field>
            <field name="res_model">hospital.patient</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'default_age': 18}</field>
            <field name="domain">[('age', '&lt;', 18)]</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">Hi, Create New Patient!</p>
            </field>
        </record>

        <record id="action_hospital_patient_dashboard" model="ir.actions.client">
            <field name="name">Patient Dashboard</field>
            <field name="tag">Hospital_mangement.patientcard_action</field>
        </record>

    </data>
</odoo>